### Building Postgres and pgAdmin

1. ```cd docker```

2. Ensure to have Docker desktop open

3. Run ```docker compose --project-name classmateai up --build```

This will build the Postgres Docker container and the pgAdmin platform using the compose.yml settings.